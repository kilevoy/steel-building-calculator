// Wind interpolation tables in both calculators support heights from 5 m,
// while the shared nu-grid caps building height at 350 m and wind spans at 400 m.
export const MIN_SUPPORTED_BUILDING_HEIGHT_M = 5
export const MAX_SUPPORTED_BUILDING_HEIGHT_M = 350
export const MAX_SUPPORTED_WIND_SPAN_M = 400
export const MAX_SUPPORTED_BUILDING_LENGTH_M = 400
